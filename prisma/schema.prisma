datasource db {
  provider = "sqlite"
  url      = "file:./cavaleiros.db"
}

model Cavaleiros {
  id        Int @id @default(autoincrement())
  nome      String
  casa      String
  armadura  String
  seculo    Int
}

generator client {
  provider = "prisma-client-js"
}
